logging:
  level:
    org:
      springframework:
        data: DEBUG

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: library-db
      username: library
      password: library